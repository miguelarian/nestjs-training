# Example secrets file for Act testing
# Copy this file to .secrets and fill in actual values
# Usage: act --secret-file .secrets

# Example secrets (these are dummy values for local testing)
CODECOV_TOKEN=dummy-token-for-local-testing
API_KEY=test-api-key-123

# Add other secrets as needed for your workflows
